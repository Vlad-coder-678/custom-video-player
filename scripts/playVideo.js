const playVideo = () => {
  video.play();
  // if (video.ended) {
  //   video.load();
  // }
  // video.play();
  // if (!isButtonPushed("play")) {
  //   pushUnpushButtons("play", []);
  //   video.play();
  // } else {
  //   pushUnpushButtons("", ["play"]);
  //   video.pause();
  // }
  // if (!isButtonPushed("pause")) {
  //   pushUnpushButtons("pause", []);
  //   video.pause();
  // } else {
  //   pushUnpushButtons("", ["pause"]);
  //   video.play();
  // }
};

export default playVideo;
